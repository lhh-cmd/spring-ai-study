# 海兴GitFlow —— 一站式 Git 分支与多环境集成管理平台

> 让「分支开发 → 环境集成 → 代码审核 → 上线发布」全流程标准化、可视化、可追踪。
> 研发团队无需再靠微信群喊话 + 手动敲 git 命令来管理分支和环境。

---

## 一、产品简介

海兴GitFlow 是一套**面向研发团队的多环境代码集成管理平台**。它以「环境」为核心把 Git 分支流转抽象成标准流程：每个项目自动维护 **dev（开发）/ test（测试）/ preview（预发）/ release（正式）** 四个公共分支，研发提交需求后，分支在环境间按序流转、合并、审核、上线，全程一键操作、全程留痕。

- 浏览器访问，**团队成员无需本地 Git 环境**，无需配置任何代理或令牌
- 真实操作远程 Git 仓库（基于 JGit），合并、推送、冲突识别均真实生效
- 深色科技风界面，操作直观，学习成本低

---

## 二、解决的痛点

| 痛点 | 海兴GitFlow 的解法 |
|---|---|
| 分支/环境靠人工协调，容易乱 | 四环境公共分支自动维护，分支集成一键完成 |
| 合到哪、谁合的、合没合，全靠问 | 待集成/已集成双列表清晰展示；全流程操作日志可追溯 |
| 代码没有门禁，直接上线 | 预发环境强制 CR 审核，不通过不能进入正式环境 |
| 合并冲突全靠个人摸索 | 自动识别冲突文件，弹窗给出分步解决指引 |
| 集成顺序错了难回退 | 退出集成/环境重建基于 master 重建公共分支，按原顺序重合并，遇冲突自动停住等解决 |
| 上线后环境状态混乱 | 合并 master 后自动重建所有环境分支，保持环境与 master 同步 |

---

## 三、功能亮点

### 1. 项目管理 —— 注册即用，零 Git 配置

- 注册项目**只需填写 Git 仓库地址**，系统使用统一服务账号操作仓库，无需每个人配置账号/令牌
- 支持真实 Git 仓库，密钥安全存储在服务端数据库，不写入任何配置文件

### 2. 需求管理 —— 建需求即建分支

- 创建需求时输入**需求名称 + 分支名称**，系统自动在远程仓库创建分支，并自动进入 dev/test/preview 三个环境的待集成列表
- 支持两种来源：**新建分支** / **从远程已有分支拉取**（兼容已在仓库中开发的分支）
- 需求状态一目了然：开发中 → 已合并 → 冲突 → 已上线

### 3. CR 代码审核 —— 上线前的门禁

- 支持发起 CR、指派审核人、填写说明
- 审核人通过/驳回，驳回后可重新发起
- **预发环境集成前强制要求 CR 通过**，保证正式环境代码经过评审

### 4. 环境管理 —— 四环境双列表

每个环境都提供 **待集成 / 已集成** 两个列表，状态一目了然：

| 状态 | 说明 |
|---|---|
| 待集成 | 该分支尚未合入当前环境公共分支，可一键集成 |
| 已集成 | 已合入当前环境，展示分支名称、需求名称、代码CR状态，可退出集成 |
| 冲突 | 合并时产生冲突，标记待解决 |

- 环境流转：**dev → test → preview → release → master**
- 支持「进入下一环境」「进入正式环境」「合并 master」等标准操作

### 5. 冲突处理 —— 自动停住，解决后继续

这是本系统的核心亮点机制：

- 按序合并分支时**遇到第一个冲突立即停住**，标记「冲突待解决」，后续分支保留在已集成列表但不实际合并
- 冲突弹窗自动列出**冲突文件清单**和**分步解决指引**
- 研发在本地按指引解决并推送后，点击【冲突已解决】→ 该分支合并成功，**系统自动继续合并后续分支**，全部完成
- 退出集成 / 环境重建均采用此机制，保证集成顺序可控、可续

### 6. 操作日志 —— 全流程审计

注册项目、创建需求、合并、冲突、CR、上线、合 master 等**所有操作**都有日志记录，包含操作人、项目、时间与详情，方便追溯与排查。

### 7. 上线闭环 —— 一键合 master

正式环境上线完成后，一键「合并 master」：上线分支与需求归档清理，四个环境分支基于最新 master 自动重建并按序重合并，保持各环境与主线同步。

---

## 四、使用流程（快速上手）

1. **注册/登录**：浏览器打开系统地址，注册账号
2. **注册项目**：填写项目名称与 Git 仓库地址
3. **创建需求**：填需求名称 + 分支名称（或从远程已有分支拉取）
4. **开发**：在本地 `feat/xxx` 分支上正常开发、提交、推送
5. **集成**：在环境管理中把分支依次集成到 dev → test → preview
6. **代码审核**：在 preview 前发起 CR，审核通过后方可进入正式环境
7. **上线**：进入正式环境（release）→ 验证完成后合并 master，流程闭环

> 全程在浏览器点按即可，团队其他人只需要登录系统，不需要懂 Git 命令行。

---

## 五、技术架构（简要）

- **后端**：Java 17 + Spring Boot 3，模块化工程
- **数据库**：MySQL
- **Git 操作**：JGit 真实操作远程仓库，支持代理访问与失败重试
- **前端**：原生 HTML/CSS/JS，深色科技风界面
- **部署**：`gitflow` 一键脚本启动/关闭/重建，内网部署后团队浏览器直接访问

---

## 六、部署说明

系统部署在一台内网机器上（当前即本机 `http://10.201.252.66:8080`），内网同事用浏览器访问即可：

```bash
# 一键启动（启动后输入 quit 关闭服务）
gitflow

# 重新打包后启动（改了代码时用）
gitflow rebuild

# 只关闭服务
gitflow stop
```

访问方式：`http://<服务器内网IP>:8080`

---

*海兴GitFlow —— 让代码集成流程简单、有序、可追溯。*
